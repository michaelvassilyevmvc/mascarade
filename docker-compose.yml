services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres_mascarade
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespw
      POSTGRES_DB: mascarade
    ports:
      - 5434:5432
    volumes:
      - ./pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
